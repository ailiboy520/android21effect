<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.effect.zy.android50effect">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme_No_Title">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".demo.ShareViewActivity"
            android:label="共享视图切换"
            android:theme="@style/AppTheme_Transitions">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="com.android5.0.effect" />
            </intent-filter>
        </activity>
        <activity
            android:name=".demo.MaterialDialogActivity"
            android:label="MaterialDialog"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="com.android5.0.effect" />
            </intent-filter>
        </activity>
    </application>

</manifest>
